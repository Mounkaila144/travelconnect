# Story 4.5: Centre de notifications

<!-- Powered by BMAD™ Core -->

## Status

**done**

---

## Story

**En tant qu'** utilisateur,
**Je veux** voir l'historique de mes notifications,
**Afin de** retrouver les alertes que j'ai manquées.

---

## Acceptance Criteria

1. Une icône cloche dans la navigation indique les notifications non lues
2. Un badge numérique montre le nombre de non lues
3. L'écran notifications liste toutes les alertes reçues
4. Les notifications non lues sont visuellement distinctes
5. Cliquer sur une notification la marque comme lue et ouvre le contenu lié
6. L'endpoint GET `/api/notifications` retourne les notifications de l'utilisateur
7. L'endpoint POST `/api/notifications/{id}/read` marque comme lue

---

## Tasks / Subtasks

### Backend Implementation Tasks

- [x] **Task 1: Create NotificationController** (AC: 6, 7)
  - [x] Create `NotificationController` in `app/Http/Controllers/Api/`
  - [x] Implement `index()` method to list user's notifications
  - [x] Implement `markAsRead()` method to mark single notification as read
  - [x] Implement `markAllAsRead()` method to mark all as read
  - [x] Create routes in routes/api.php:
    - GET /api/notifications
    - POST /api/notifications/{id}/read
    - POST /api/notifications/read-all
  - [x] Apply auth:sanctum and not.banned middleware

- [x] **Task 2: Implement index method for listing notifications** (AC: 3, 6)
  - [x] Query notifications for authenticated user
  - [x] Order by created_at DESC (most recent first)
  - [x] Paginate results (20 per page)
  - [x] Include unread_count in response
  - [x] Return NotificationResource collection

- [x] **Task 3: Implement markAsRead method** (AC: 5, 7)
  - [x] Validate notification belongs to authenticated user
  - [x] Find notification by ID
  - [x] Return 404 if not found or doesn't belong to user
  - [x] Update read_at to current timestamp
  - [x] Return 204 No Content on success

- [x] **Task 4: Implement markAllAsRead method** (AC: 7)
  - [x] Update all unread notifications for authenticated user
  - [x] Set read_at to current timestamp
  - [x] Return count of marked notifications
  - [x] Return 200 OK with count

- [x] **Task 5: Create NotificationResource** (AC: 3, 6)
  - [x] Create `NotificationResource` in `app/Http/Resources/`
  - [x] Include all notification fields (id, type, title, body, data, read_at, created_at)
  - [x] Format created_at as ISO 8601
  - [x] Add time_ago field for relative time
  - [x] Parse data field as object (not string)

- [x] **Task 6: Add unread count to response** (AC: 2)
  - [x] In NotificationController@index
  - [x] Calculate unread count for user
  - [x] Add to response via ->additional(['unread_count' => $count])

- [x] **Task 7: Create endpoint to get unread count only** (AC: 2)
  - [x] Create `getUnreadCount()` method in NotificationController
  - [x] Create route GET /api/notifications/unread-count
  - [x] Return simple JSON: `{"unread_count": N}`
  - [x] Apply auth middleware

- [x] **Task 8: Optimize notification queries** (AC: 3, 6)
  - [x] Indexes on (user_id, read_at) and (user_id, created_at) exist from initial migration

- [x] **Task 9: Create backend tests** (Testing)
  - [x] Create `NotificationControllerTest` in `tests/Feature/Notifications/`
  - [x] Test GET /api/notifications returns user's notifications only
  - [x] Test notifications ordered by created_at DESC
  - [x] Test pagination works correctly
  - [x] Test unread_count is accurate
  - [x] Test POST /api/notifications/{id}/read marks as read
  - [x] Test cannot mark other user's notification as read (404)
  - [x] Test POST /api/notifications/read-all marks all as read
  - [x] Test unauthenticated requests return 401
  - [x] Test GET /api/notifications/unread-count returns count
  - [x] Test response structure matches spec

### Frontend Implementation Tasks

- [x] **Task 10: Add notifications icon to navigation** (AC: 1, 2)
  - [x] Add notification icon to BottomNavigationBar (NavigationBar)
  - [x] Use Icons.notifications / Icons.notifications_outlined
  - [x] Add NotificationBadge overlay showing unread count
  - [x] Hide badge when count is 0
  - [x] Tap icon switches to NotificationsPage tab

- [x] **Task 11: Create NotificationsPage UI** (AC: 3, 4, 5)
  - [x] Create `NotificationsPage` in `lib/features/notifications/presentation/pages/`
  - [x] Add AppBar with title "Notifications"
  - [x] Add "Tout lire" action button when unread count > 0
  - [x] Display ListView of NotificationItem widgets
  - [x] Show loading indicator while fetching
  - [x] Show empty state when no notifications
  - [x] Apply Material Design 3 styling

- [x] **Task 12: Create NotificationItem widget** (AC: 4, 5)
  - [x] Create `NotificationItem` in widgets folder
  - [x] Display notification icon based on type (question_answer, location_on)
  - [x] Display title (bold for unread)
  - [x] Display body text (2 lines max)
  - [x] Display time ago
  - [x] Show unread indicator (blue background tint + blue dot)
  - [x] Add tap gesture to handle notification action

- [x] **Task 13: Create NotificationsCenterBloc for state management** (AC: All)
  - [x] Create `NotificationsCenterBloc` in presentation/bloc/
  - [x] Define events: LoadNotifications, RefreshNotifications, MarkAsRead, MarkAllAsRead, UpdateUnreadCount
  - [x] Define states: Initial, Loading, Loaded (with copyWith), Error
  - [x] Implement optimistic updates with revert on failure

- [x] **Task 14: Implement notifications loading** (AC: 3, 6)
  - [x] Call GET /api/notifications endpoint
  - [x] Parse response into AppNotification entities
  - [x] Extract unread count from response
  - [x] Update state with notifications and count

- [x] **Task 15: Implement mark as read functionality** (AC: 5, 7)
  - [x] Optimistic update: mark read + decrement count
  - [x] Revert on API failure
  - [x] Navigate to question detail based on notification type/data

- [x] **Task 16: Implement mark all as read** (AC: 7)
  - [x] "Tout lire" TextButton in AppBar
  - [x] Optimistic update: mark all read + set count to 0
  - [x] Revert on API failure

- [x] **Task 17: Create notifications repository and datasource** (AC: All)
  - [x] Create `NotificationsRemoteDataSource` with getNotifications, markAsRead, markAllAsRead, getUnreadCount
  - [x] Create `NotificationsRepository` interface + NotificationsResult class
  - [x] Create `NotificationsRepositoryImpl`
  - [x] Create `AppNotification` entity + `NotificationModel`

- [ ] **Task 18: Implement unread count polling** (AC: 2) — Deferred
  - [ ] Polling service deferred; unread count updated via BLoC events on page load

- [x] **Task 19: Handle notification navigation** (AC: 5)
  - [x] Parse notification type and data
  - [x] For 'new_answer' and 'nearby_question': navigate to `/question/{id}`
  - [x] Extract question_id from notification data
  - [x] Mark notification as read before navigation

- [x] **Task 20: Implement pull-to-refresh** (AC: 3)
  - [x] Wrap ListView with RefreshIndicator
  - [x] Trigger RefreshNotifications event on pull

- [x] **Task 21: Create frontend tests** (Testing)
  - [x] Create bloc test for NotificationsCenterBloc (9 test cases)
  - [x] Test LoadNotifications success and error
  - [x] Test MarkAsRead decrements unread count
  - [x] Test MarkAsRead does not decrement for already-read
  - [x] Test MarkAllAsRead sets all to read
  - [x] Test MarkAsRead reverts on failure
  - [x] Test UpdateUnreadCount updates count
  - [x] Test RefreshNotifications calls API (verify approach)

### Integration & Verification Tasks

- [x] **Task 22: Setup dependency injection** (AC: All)
  - [x] Register NotificationsCenterBloc in DI (injection.dart)
  - [x] Register NotificationsRemoteDataSource and NotificationsRepository
  - [x] Provide NotificationsCenterBloc globally in MultiBlocProvider (main.dart)

- [ ] **Task 23: End-to-end testing** (AC: All) — Manual testing required
  - [ ] Receive push notification (create answer to own question from another device)
  - [ ] Verify notification icon shows badge with count 1
  - [ ] Tap notification icon
  - [ ] Verify NotificationsPage opens
  - [ ] Verify notification appears in list with unread styling
  - [ ] Tap notification item
  - [ ] Verify notification marked as read (styling changes)
  - [ ] Verify navigation to question detail
  - [ ] Verify badge count decrements to 0
  - [ ] Go back to notifications page
  - [ ] Receive 3 more notifications
  - [ ] Verify badge shows count 3
  - [ ] Tap "Tout marquer comme lu"
  - [ ] Verify all notifications marked as read
  - [ ] Verify badge disappears (count 0)
  - [ ] Pull down to refresh
  - [ ] Verify notifications reload
  - [ ] Test with no internet connection
  - [ ] Verify error message and retry option

---

## Dev Notes

### Previous Story Insights

[Source: Stories 4.1, 4.2, 4.3 Completion]
- Notifications table should exist with all fields
- NotificationService should have createNotification method
- FCM notifications should be sending successfully
- Notification types: 'new_answer', 'nearby_question'
- Notification data should contain relevant IDs for navigation

### API Endpoint Specification

[Source: architecture/5-spécification-api-rest.md#5.3.7]

**List Notifications Endpoint:**

**Endpoint:** `GET /api/notifications`

**Query Parameters:**
- `page`: optional, integer (default: 1)

**Response (200 OK):**
```json
{
  "data": [
    {
      "id": "uuid-here",
      "type": "new_answer",
      "title": "Nouvelle réponse",
      "body": "Tanaka a répondu à votre question",
      "data": {
        "question_id": 1,
        "answer_id": 5
      },
      "read_at": null,
      "created_at": "2026-01-31T10:00:00Z",
      "time_ago": "il y a 2h"
    },
    {
      "id": "uuid-here-2",
      "type": "nearby_question",
      "title": "Nouvelle question près de Shibuya",
      "body": "Best sushi restaurant?",
      "data": {
        "question_id": 8
      },
      "read_at": "2026-01-31T11:00:00Z",
      "created_at": "2026-01-31T09:30:00Z",
      "time_ago": "il y a 3h"
    }
  ],
  "meta": {
    "current_page": 1,
    "last_page": 3,
    "per_page": 20,
    "total": 52
  },
  "unread_count": 3
}
```

**Mark as Read Endpoint:**

**Endpoint:** `POST /api/notifications/{id}/read`

**Response (204 No Content):**
No body.

**Error Responses:**
- `404 Not Found` - Notification doesn't exist or doesn't belong to user

**Mark All as Read Endpoint:**

**Endpoint:** `POST /api/notifications/read-all`

**Response (200 OK):**
```json
{
  "marked_count": 5
}
```

**Get Unread Count Endpoint:**

**Endpoint:** `GET /api/notifications/unread-count`

**Response (200 OK):**
```json
{
  "unread_count": 3
}
```

### Backend NotificationController Implementation

[Source: architecture/11-architecture-backend-laravel.md#11.2]

**File Location:** `app/Http/Controllers/Api/NotificationController.php`

**Complete Implementation:**

```php
<?php

namespace App\Http\Controllers\Api;

use App\Http\Controllers\Controller;
use App\Http\Resources\NotificationResource;
use App\Models\Notification;
use App\Services\NotificationService;
use Illuminate\Http\JsonResponse;
use Illuminate\Http\Request;
use Illuminate\Http\Resources\Json\AnonymousResourceCollection;

class NotificationController extends Controller
{
    public function __construct(
        private readonly NotificationService $notificationService
    ) {}

    /**
     * List all notifications for authenticated user
     */
    public function index(Request $request): AnonymousResourceCollection
    {
        $user = $request->user();

        $notifications = Notification::where('user_id', $user->id)
            ->orderByDesc('created_at')
            ->paginate(20);

        $unreadCount = $this->notificationService->getUnreadCount($user->id);

        return NotificationResource::collection($notifications)
            ->additional([
                'unread_count' => $unreadCount,
            ]);
    }

    /**
     * Mark single notification as read
     */
    public function markAsRead(string $id, Request $request): JsonResponse
    {
        $user = $request->user();

        $notification = Notification::where('id', $id)
            ->where('user_id', $user->id)
            ->first();

        if (!$notification) {
            return response()->json([
                'error' => [
                    'code' => 'NOT_FOUND',
                    'message' => 'Notification non trouvée',
                ],
            ], 404);
        }

        $this->notificationService->markAsRead($id);

        return response()->json(null, 204);
    }

    /**
     * Mark all notifications as read for authenticated user
     */
    public function markAllAsRead(Request $request): JsonResponse
    {
        $user = $request->user();

        $markedCount = $this->notificationService->markAllAsRead($user->id);

        return response()->json([
            'marked_count' => $markedCount,
        ]);
    }

    /**
     * Get unread count only (for polling)
     */
    public function getUnreadCount(Request $request): JsonResponse
    {
        $user = $request->user();
        $unreadCount = $this->notificationService->getUnreadCount($user->id);

        return response()->json([
            'unread_count' => $unreadCount,
        ]);
    }
}
```

### NotificationResource Implementation

**File Location:** `app/Http/Resources/NotificationResource.php`

```php
<?php

namespace App\Http\Resources;

use Illuminate\Http\Resources\Json\JsonResource;

class NotificationResource extends JsonResource
{
    public function toArray($request): array
    {
        return [
            'id' => $this->id,
            'type' => $this->type,
            'title' => $this->title,
            'body' => $this->body,
            'data' => $this->data, // Already cast as array in model
            'read_at' => $this->read_at?->toIso8601String(),
            'created_at' => $this->created_at->toIso8601String(),
            'time_ago' => $this->getTimeAgo(),
        ];
    }

    private function getTimeAgo(): string
    {
        $diff = now()->diffInSeconds($this->created_at);

        if ($diff < 60) {
            return 'à l\'instant';
        } elseif ($diff < 3600) {
            $minutes = floor($diff / 60);
            return "il y a {$minutes} min";
        } elseif ($diff < 86400) {
            $hours = floor($diff / 3600);
            return "il y a {$hours} h";
        } elseif ($diff < 2592000) {
            $days = floor($diff / 86400);
            return "il y a {$days} j";
        } else {
            return $this->created_at->format('d M');
        }
    }
}
```

### Routes Configuration

**File Location:** `routes/api.php`

```php
// Notification routes
Route::middleware(['auth:sanctum', 'not.banned'])->group(function () {
    Route::get('/notifications', [NotificationController::class, 'index']);
    Route::get('/notifications/unread-count', [NotificationController::class, 'getUnreadCount']);
    Route::post('/notifications/{id}/read', [NotificationController::class, 'markAsRead']);
    Route::post('/notifications/read-all', [NotificationController::class, 'markAllAsRead']);
});
```

### Database Schema

[Source: architecture/9-schéma-de-base-de-données.md]

**Notifications Table (already created in Story 4.2):**
```sql
CREATE TABLE notifications (
    id CHAR(36) PRIMARY KEY,
    user_id BIGINT UNSIGNED NOT NULL,
    type VARCHAR(50) NOT NULL,
    title VARCHAR(255) NOT NULL,
    body TEXT NOT NULL,
    data JSON NULL,
    read_at TIMESTAMP NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    INDEX idx_user_read (user_id, read_at),
    INDEX idx_user_created (user_id, created_at DESC)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### Frontend NotificationsPage Implementation

[Source: architecture/10-architecture-frontend-flutter.md]

**File Location:** `lib/features/notifications/presentation/pages/notifications_page.dart`

**Complete Implementation:**

```dart
import 'package:flutter/material.dart';
import 'package:flutter_bloc/flutter_bloc.dart';
import 'package:go_router/go_router.dart';

class NotificationsPage extends StatefulWidget {
  const NotificationsPage({Key? key}) : super(key: key);

  @override
  State<NotificationsPage> createState() => _NotificationsPageState();
}

class _NotificationsPageState extends State<NotificationsPage> {
  @override
  void initState() {
    super.initState();
    context.read<NotificationsBloc>().add(LoadNotifications());
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Notifications'),
        actions: [
          BlocBuilder<NotificationsBloc, NotificationsState>(
            builder: (context, state) {
              if (state is NotificationsLoaded && state.unreadCount > 0) {
                return TextButton(
                  onPressed: () {
                    context.read<NotificationsBloc>().add(MarkAllAsRead());
                  },
                  child: const Text('Tout marquer comme lu'),
                );
              }
              return const SizedBox.shrink();
            },
          ),
        ],
      ),
      body: BlocConsumer<NotificationsBloc, NotificationsState>(
        listener: (context, state) {
          if (state is NotificationMarkedAsRead) {
            // Navigate to linked content
            _handleNotificationNavigation(state.notification);
          }
        },
        builder: (context, state) {
          if (state is NotificationsLoading && state.notifications.isEmpty) {
            return const Center(child: CircularProgressIndicator());
          }

          if (state is NotificationsError) {
            return Center(
              child: Column(
                mainAxisAlignment: MainAxisAlignment.center,
                children: [
                  Icon(Icons.error_outline, size: 64, color: Colors.grey),
                  const SizedBox(height: 16),
                  Text(state.message),
                  const SizedBox(height: 16),
                  ElevatedButton(
                    onPressed: () {
                      context.read<NotificationsBloc>().add(LoadNotifications());
                    },
                    child: const Text('Réessayer'),
                  ),
                ],
              ),
            );
          }

          final notifications = state is NotificationsLoaded
              ? state.notifications
              : [];

          if (notifications.isEmpty) {
            return const Center(
              child: Column(
                mainAxisAlignment: MainAxisAlignment.center,
                children: [
                  Icon(Icons.notifications_none, size: 64, color: Colors.grey),
                  SizedBox(height: 16),
                  Text('Aucune notification'),
                ],
              ),
            );
          }

          return RefreshIndicator(
            onRefresh: () async {
              context.read<NotificationsBloc>().add(RefreshNotifications());
              await Future.delayed(const Duration(seconds: 1));
            },
            child: ListView.separated(
              itemCount: notifications.length,
              separatorBuilder: (context, index) => const Divider(height: 1),
              itemBuilder: (context, index) {
                final notification = notifications[index];
                return NotificationItem(
                  notification: notification,
                  onTap: () {
                    context.read<NotificationsBloc>().add(
                      NotificationTapped(notification),
                    );
                  },
                );
              },
            ),
          );
        },
      ),
    );
  }

  void _handleNotificationNavigation(Notification notification) {
    final data = notification.data;

    switch (notification.type) {
      case 'new_answer':
      case 'nearby_question':
        final questionId = data['question_id'];
        if (questionId != null) {
          context.push('/question/$questionId');
        }
        break;
      default:
        // Unknown notification type, do nothing
        break;
    }
  }
}
```

### NotificationItem Widget

**File Location:** `lib/features/notifications/presentation/widgets/notification_item.dart`

```dart
import 'package:flutter/material.dart';
import 'package:travelconnect/features/notifications/domain/entities/notification.dart';

class NotificationItem extends StatelessWidget {
  final Notification notification;
  final VoidCallback onTap;

  const NotificationItem({
    Key? key,
    required this.notification,
    required this.onTap,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    final theme = Theme.of(context);
    final isUnread = notification.readAt == null;

    return InkWell(
      onTap: onTap,
      child: Container(
        color: isUnread
            ? theme.colorScheme.primary.withOpacity(0.05)
            : Colors.transparent,
        padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 12),
        child: Row(
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            // Notification icon
            Container(
              padding: const EdgeInsets.all(8),
              decoration: BoxDecoration(
                color: _getIconColor(notification.type).withOpacity(0.1),
                borderRadius: BorderRadius.circular(8),
              ),
              child: Icon(
                _getIcon(notification.type),
                color: _getIconColor(notification.type),
                size: 24,
              ),
            ),
            const SizedBox(width: 12),

            // Notification content
            Expanded(
              child: Column(
                crossAxisAlignment: CrossAxisAlignment.start,
                children: [
                  // Title
                  Text(
                    notification.title,
                    style: theme.textTheme.titleSmall?.copyWith(
                      fontWeight: isUnread ? FontWeight.bold : FontWeight.normal,
                    ),
                  ),
                  const SizedBox(height: 4),

                  // Body
                  Text(
                    notification.body,
                    style: theme.textTheme.bodyMedium?.copyWith(
                      color: Colors.grey[700],
                    ),
                    maxLines: 2,
                    overflow: TextOverflow.ellipsis,
                  ),
                  const SizedBox(height: 4),

                  // Time
                  Text(
                    notification.timeAgo,
                    style: theme.textTheme.bodySmall?.copyWith(
                      color: Colors.grey[600],
                    ),
                  ),
                ],
              ),
            ),

            // Unread indicator
            if (isUnread) ...[
              const SizedBox(width: 8),
              Container(
                width: 8,
                height: 8,
                decoration: BoxDecoration(
                  color: theme.colorScheme.primary,
                  shape: BoxShape.circle,
                ),
              ),
            ],
          ],
        ),
      ),
    );
  }

  IconData _getIcon(String type) {
    switch (type) {
      case 'new_answer':
        return Icons.question_answer;
      case 'nearby_question':
        return Icons.location_on;
      default:
        return Icons.notifications;
    }
  }

  Color _getIconColor(String type) {
    switch (type) {
      case 'new_answer':
        return Colors.blue;
      case 'nearby_question':
        return Colors.green;
      default:
        return Colors.grey;
    }
  }
}
```

### NotificationBadge Widget

**File Location:** `lib/features/notifications/presentation/widgets/notification_badge.dart`

```dart
import 'package:flutter/material.dart';
import 'package:flutter_bloc/flutter_bloc.dart';

class NotificationBadge extends StatelessWidget {
  final Widget child;

  const NotificationBadge({
    Key? key,
    required this.child,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return BlocBuilder<NotificationsBloc, NotificationsState>(
      builder: (context, state) {
        final unreadCount = state is NotificationsLoaded
            ? state.unreadCount
            : 0;

        if (unreadCount == 0) {
          return child;
        }

        return Badge(
          label: Text(
            unreadCount > 99 ? '99+' : '$unreadCount',
            style: const TextStyle(fontSize: 10),
          ),
          backgroundColor: Colors.red,
          child: child,
        );
      },
    );
  }
}
```

### Unread Count Polling Service

**File Location:** `lib/core/services/notification_polling_service.dart`

```dart
import 'dart:async';
import 'package:flutter_bloc/flutter_bloc.dart';

class NotificationPollingService {
  Timer? _timer;
  final NotificationsBloc _notificationsBloc;
  final NotificationsRepository _repository;

  NotificationPollingService(
    this._notificationsBloc,
    this._repository,
  );

  void startPolling() {
    _timer?.cancel();
    _timer = Timer.periodic(const Duration(seconds: 30), (_) async {
      try {
        final count = await _repository.getUnreadCount();
        _notificationsBloc.add(UpdateUnreadCount(count));
      } catch (e) {
        // Silently fail for polling errors
        print('Error polling unread count: $e');
      }
    });
  }

  void stopPolling() {
    _timer?.cancel();
    _timer = null;
  }

  void dispose() {
    _timer?.cancel();
  }
}
```

### Error Handling Strategy

[Source: architecture/18-stratégie-de-gestion-derreurs.md]

**Backend Error Scenarios:**
- Notification not found (return 404)
- Notification belongs to different user (return 404, not 403 for security)
- Database timeout (return 500, log error)
- Invalid notification ID format (return 422)

**Frontend Error Scenarios:**
- Network error loading notifications (show error with retry)
- Empty notifications list (show empty state)
- Mark as read fails (show snackbar, retry)
- Navigation fails (show error message)

**User-Friendly Error Messages:**
- Load error: "Impossible de charger les notifications. Vérifiez votre connexion."
- Empty state: "Aucune notification pour l'instant."
- Mark as read error: "Erreur lors du marquage. Réessayez."
- Navigation error: "Impossible d'ouvrir le contenu. Réessayez."

---

## Dev Notes - Testing

### Testing Standards

[Source: architecture/16-stratégie-de-tests.md]

**Backend Testing Requirements:**

**Test File:** `tests/Feature/NotificationControllerTest.php`

**Required Test Cases:**
1. `test_user_can_list_their_notifications()`
   - Create notifications for user
   - Request GET /api/notifications
   - Verify only user's notifications returned
   - Verify ordered by created_at DESC

2. `test_notifications_include_unread_count()`
   - Create 3 unread and 2 read notifications
   - Request notifications
   - Verify unread_count is 3

3. `test_user_can_mark_notification_as_read()`
   - Create unread notification
   - Request POST /api/notifications/{id}/read
   - Verify read_at is set
   - Verify 204 status

4. `test_user_cannot_mark_other_users_notification_as_read()`
   - Create notification for User A
   - Login as User B
   - Try to mark A's notification as read
   - Verify 404 status

5. `test_user_can_mark_all_notifications_as_read()`
   - Create 5 unread notifications
   - Request POST /api/notifications/read-all
   - Verify all marked as read
   - Verify marked_count is 5

6. `test_pagination_works_correctly()`
   - Create 30 notifications
   - Request page 1
   - Verify 20 items returned
   - Request page 2
   - Verify 10 items returned

**Example Test:**
```php
<?php

namespace Tests\Feature;

use App\Models\Notification;
use App\Models\User;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Illuminate\Support\Str;
use Tests\TestCase;

class NotificationControllerTest extends TestCase
{
    use RefreshDatabase;

    public function test_user_can_list_their_notifications(): void
    {
        $user = User::factory()->create();
        $otherUser = User::factory()->create();

        // Create notifications for current user
        $notification1 = Notification::factory()->create([
            'user_id' => $user->id,
            'created_at' => now()->subHours(1),
        ]);

        $notification2 = Notification::factory()->create([
            'user_id' => $user->id,
            'created_at' => now()->subHours(2),
        ]);

        // Create notification for other user (should not appear)
        Notification::factory()->create([
            'user_id' => $otherUser->id,
        ]);

        $response = $this->actingAs($user)->getJson('/api/notifications');

        $response->assertStatus(200)
            ->assertJsonStructure([
                'data' => [
                    '*' => ['id', 'type', 'title', 'body', 'data', 'read_at', 'created_at', 'time_ago'],
                ],
                'meta',
                'unread_count',
            ])
            ->assertJsonCount(2, 'data');

        // Verify order (newest first)
        $notifications = $response->json('data');
        $this->assertEquals($notification1->id, $notifications[0]['id']);
        $this->assertEquals($notification2->id, $notifications[1]['id']);
    }

    public function test_user_can_mark_notification_as_read(): void
    {
        $user = User::factory()->create();

        $notification = Notification::factory()->create([
            'user_id' => $user->id,
            'read_at' => null,
        ]);

        $response = $this->actingAs($user)
            ->postJson("/api/notifications/{$notification->id}/read");

        $response->assertStatus(204);

        $this->assertDatabaseHas('notifications', [
            'id' => $notification->id,
        ]);

        $notification->refresh();
        $this->assertNotNull($notification->read_at);
    }

    public function test_user_can_mark_all_notifications_as_read(): void
    {
        $user = User::factory()->create();

        // Create 5 unread notifications
        Notification::factory()->count(5)->create([
            'user_id' => $user->id,
            'read_at' => null,
        ]);

        $response = $this->actingAs($user)
            ->postJson('/api/notifications/read-all');

        $response->assertStatus(200)
            ->assertJson(['marked_count' => 5]);

        $this->assertEquals(0, Notification::where('user_id', $user->id)
            ->whereNull('read_at')
            ->count());
    }
}
```

**Frontend Testing Requirements:**

**Widget Test:** `test/widget/notifications_page_test.dart`
- Test page displays notifications list
- Test empty state shows when no notifications
- Test unread notifications have distinct styling
- Test "Tout marquer comme lu" button appears when unread count > 0
- Test pull-to-refresh triggers refresh

**Widget Test:** `test/widget/notification_item_test.dart`
- Test unread notification has blue background
- Test read notification has transparent background
- Test unread indicator dot shows for unread
- Test correct icon displays for each notification type
- Test tapping notification calls onTap callback

**Bloc Test:** `test/unit/bloc/notifications_bloc_test.dart`
- Test LoadNotifications fetches and emits loaded state
- Test MarkAsRead updates notification in state
- Test MarkAllAsRead sets all notifications to read
- Test unread count updates correctly

**Example Test:**
```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:bloc_test/bloc_test.dart';
import 'package:mockito/mockito.dart';

void main() {
  group('NotificationsBloc', () {
    late MockNotificationsRepository mockRepository;
    late NotificationsBloc bloc;

    setUp(() {
      mockRepository = MockNotificationsRepository();
      bloc = NotificationsBloc(notificationsRepository: mockRepository);
    });

    blocTest<NotificationsBloc, NotificationsState>(
      'emits [Loading, Loaded] when LoadNotifications succeeds',
      build: () {
        when(mockRepository.getNotifications(page: 1))
            .thenAnswer((_) async => mockNotificationsList);
        when(mockRepository.getUnreadCount())
            .thenAnswer((_) async => 3);
        return bloc;
      },
      act: (bloc) => bloc.add(LoadNotifications()),
      expect: () => [
        NotificationsLoading(notifications: [], unreadCount: 0),
        NotificationsLoaded(
          notifications: mockNotificationsList,
          unreadCount: 3,
        ),
      ],
    );

    blocTest<NotificationsBloc, NotificationsState>(
      'MarkAllAsRead sets all notifications to read',
      build: () {
        when(mockRepository.markAllAsRead())
            .thenAnswer((_) async => 5);
        return bloc;
      },
      seed: () => NotificationsLoaded(
        notifications: mockUnreadNotifications,
        unreadCount: 5,
      ),
      act: (bloc) => bloc.add(MarkAllAsRead()),
      expect: () => [
        NotificationsLoaded(
          notifications: mockReadNotifications,
          unreadCount: 0,
        ),
      ],
      verify: (_) {
        verify(mockRepository.markAllAsRead()).called(1);
      },
    );
  });
}
```

**Test Execution:**
```bash
# Backend
php artisan test tests/Feature/NotificationControllerTest.php

# Frontend
flutter test test/widget/notifications_page_test.dart
flutter test test/widget/notification_item_test.dart
flutter test test/unit/bloc/notifications_bloc_test.dart
```

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-01-31 | 1.0 | Initial story creation | Scrum Master |

---

## Dev Agent Record

### Agent Model Used

Claude Opus 4.5 (claude-opus-4-5-20251101)

### Completion Notes List

- Backend: NotificationController with index (paginated, with unread_count), markAsRead (404 if not owner), markAllAsRead (returns marked_count), getUnreadCount. NotificationResource with time_ago. NotificationFactory with read() and nearbyQuestion() states.
- Frontend: NotificationsCenterBloc with optimistic updates and revert on failure for MarkAsRead/MarkAllAsRead. AppNotification entity (named to avoid Flutter Notification collision). NotificationBadge widget in BottomNavigationBar. NotificationItem with type-based icons and unread indicator.
- Task 18 (polling service) deferred — unread count refreshed on page load and after mark actions. Can add periodic polling later.
- Notifications tab added to BottomNavigationBar with NotificationBadge overlay.
- Backend tests: 10/10 pass (NotificationControllerTest.php)
- Frontend tests: 9/9 pass (notifications_center_bloc_test.dart) using mocktail
- Equatable state deduplication required verify-based approach for RefreshNotifications test

### File List

**Backend (Laravel):**
- `travelconnect-api/app/Http/Controllers/Api/NotificationController.php` (new)
- `travelconnect-api/app/Http/Resources/NotificationResource.php` (new)
- `travelconnect-api/app/Models/Notification.php` (new — HasFactory, HasUuids, data cast)
- `travelconnect-api/app/Services/NotificationService.php` (modified — added getUnreadCount, markAsRead, markAllAsRead)
- `travelconnect-api/database/factories/NotificationFactory.php` (new — with read() and nearbyQuestion() states)
- `travelconnect-api/routes/api.php` (modified — added notification CRUD routes)
- `travelconnect-api/tests/Feature/Notifications/NotificationControllerTest.php` (new — 10 tests)

**Frontend (Flutter):**
- `travelconnect-app/lib/features/notifications/domain/entities/app_notification.dart` (new)
- `travelconnect-app/lib/features/notifications/data/models/notification_model.dart` (new)
- `travelconnect-app/lib/features/notifications/data/datasources/notifications_remote_datasource.dart` (new)
- `travelconnect-app/lib/features/notifications/domain/repositories/notifications_repository.dart` (new)
- `travelconnect-app/lib/features/notifications/data/repositories/notifications_repository_impl.dart` (new)
- `travelconnect-app/lib/features/notifications/presentation/bloc/notifications_center_bloc.dart` (new)
- `travelconnect-app/lib/features/notifications/presentation/bloc/notifications_center_event.dart` (new)
- `travelconnect-app/lib/features/notifications/presentation/bloc/notifications_center_state.dart` (new)
- `travelconnect-app/lib/features/notifications/presentation/pages/notifications_page.dart` (new)
- `travelconnect-app/lib/features/notifications/presentation/widgets/notification_item.dart` (new)
- `travelconnect-app/lib/features/notifications/presentation/widgets/notification_badge.dart` (new)
- `travelconnect-app/lib/injection.dart` (modified — registered NotificationsRemoteDataSource, NotificationsRepository, NotificationsCenterBloc)
- `travelconnect-app/lib/main.dart` (modified — added NotificationsCenterBloc to MultiBlocProvider, added _HomePage with NavigationBar)
- `travelconnect-app/test/unit/bloc/notifications_center_bloc_test.dart` (new — 9 tests)

---

## QA Results

Not yet implemented
